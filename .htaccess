# Redirect URLs to not have the .html extension
RewriteEngine On
RewriteCond %{REQUEST_URI} !/(static|img)/
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME}\.html -f
RewriteRule ^(.*)$ $1.html
